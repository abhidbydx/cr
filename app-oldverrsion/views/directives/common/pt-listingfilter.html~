<div class="searchBar">
	<div class="row-fluid searchFilter" style="display:block;">
        	<ul class="nav pull-left">
                <li>
                    <div class="input-append searchBox">
                        <input type="text" 
                                ng-model="selValue"
                                ng-keypress="formFill()" 
                                typeahead="result.displayText for result in searchSuggestions($viewValue) | limitTo:8" 
                                typeahead-template-url="views/static/searchTypeAhead.html"
                                typeahead-on-select="searchOptionSelected($item, $model, $label)"
                                typeahead-input-formatter="$model = ''" pt-placeholder="Enter a locality, builder or project" />
                        <button type="button" ng-click="setDefaultSort()" class="btn btn-small search-bg"><i class="fa fa-search"></i></button>
                    </div>
                </li>
            </ul>
            <ul class="nav pull-left" ng-controller="filterCtrl" ng-if="showFilters">
                <li class="dropdown">
                    <a href="javascript:void(0)" class="dropdown-btn">
                    	<b ng-if="filters.bed.selected.length != 0">{{getAppliedFilters('bed')}} Bed(s) <i class="caret"></i></b>
                        <span ng-if="filters.bed.selected.length == 0">Bed(s) <i class="caret"></i></span>
                    </a>
                    <ul class="dropdown-content">
                        <li class="bed-filter">
                            <div pt-multiselect data="filters.bed.data" selected="filters.bed.selection" name="bed" update-filter="updateFilter(filter='bed', fetch='true', data)" any="false"> 
                                <button class="btn btn-mini clearFilter" ng-click="clearBed()">CLEAR</button>
                            </div>
                        </li>
                   </ul>
                </li> 
                <li class="dropdown">
                    <a href="javascript:void(0)" ng-if="filters.budget.selection[0] && filters.budget.selection[1]" class="dropdown-btn">
                        <b ng-if="filters.budget.selection[0] != filters.budget.data[0] || filters.budget.selection[1] != filters.budget.data[1]"><i class="fa fa-inr"></i>{{ filters.budget.selection[0] | priceFormat }} - &nbsp;<i class="fa fa-inr"></i>{{ filters.budget.selection[1] | priceFormat }}</b>
                        <span ng-if="filters.budget.selection[0] == filters.budget.data[0] && filters.budget.selection[1] == filters.budget.data[1]">Budget</span>
                        <i class="caret"></i></a>
    
                    <ul class="dropdown-content filter-budget">
                        <li>
                        <i class="fa fa-inr"></i><span class="budget-min-size">{{ filters.budget.selection[0] | priceFormat }}</span>
                            <div class="filter-slider" pt-rangeslider range="filters.budget.data" selected="filters.budget.selection" name="budget" update-filter="updateFilter(filter='budget', fetch='true', data)"></div>
                        <i class="fa fa-inr"></i><span class="budget-max-size">{{ filters.budget.selection[1] | priceFormat }}</span>
                        </li>
                    </ul>
                </li> 
                <li class="dropdown">
                    <a href="javascript:void(0)" class="dropdown-btn">
                        <b ng-if="displaySettings.appliedMoreFilter">More ({{displaySettings.appliedMoreFilter}})</b>
                    	<span ng-if="!displaySettings.appliedMoreFilter">More </span>
                        <i class="caret"></i>
                    </a>
                    <ul class="dropdown-content filter-more">
                        <li>
                            <div class="more-filter-blk propertyType-filter">
                                <label>Property Type</label>
                                <div class="proptype-filter" pt-multiselect data="filters.propertyType.data" name="propertyType" selected="filters.propertyType.selection" update-filter="updateFilter(filter='propertyType', fetch='true', data)" any="false">
                                </div>
                            </div>
                            <div class="more-filter-blk bath-filter">
                                <label>Baths</label>
                                <div class="bath-filter" pt-multiselect data="filters.bath.data" name="bath" selected="filters.bath.selection" update-filter="updateFilter(filter='bath', fetch='true', data)" any="false">
                                </div>
                            </div>
                            <div class="more-filter-blk listingType-filter">
                                <label>Listing Type</label>
                                <div class="proptype-filter" pt-multiselect data="filters.listingType.data" name="listingType" selected="filters.listingType.selection" update-filter="updateFilter(filter='listingType', fetch='true', data)" any="false">
                                </div>
                            </div>
                            <div class="more-filter-blk status-filter">
                                <label>Project Status</label>
                                <div class="proptype-filter" pt-multiselect data="filters.projectStatus.data" name="projectStatus" selected="filters.projectStatus.selection" update-filter="updateFilter(filter='projectStatus', fetch='true', data)" any="false">
                                </div>
                            </div>
                            <div class="more-filter-blk possessionDate-filter float-label">
                                <label>Possession Within Next</label>
                                <div class="bath-filter" pt-singleselect data="filters.completionDate.data" name="possessionTime" selected="filters.completionDate.selection" update-filter="updateFilter(filter='completionDate', fetch='true', data)" >
                                </div>
                            </div>
                            <div class="more-filter-blk size-filter">
                                <label>AREA SIZE</label>
    
                                
                                <span class="sqft_wrap"><span class="area-min-size">{{filters.size.selection[0]}} </span><div class="clear">sq ft</div></span>
                                <div class="filter-slider" pt-rangeslider range="filters.size.data" selected="filters.size.selection" name="area" update-filter="updateFilter(filter='size', fetch='true', data)"></div>                            
                                <span class="sqft_wrap"><span class="area-max-size">{{filters.size.selection[1]}} </span><div class="clear">sq ft</div></span>
    
                            </div>
                            <!--<div class="more-filter-blk size-filter">
                                <label>{{filters.size.title}}</label>
                                <div class="size-filter">
                                    <br>
                                </div>
                            </div>-->
                        </li>
                        <li>
                            <!--<div class="column brdr-top brdr-right">
                                <div class="locality-filter">
                                    <label>{{filters.locality.title}}</label>
                                    <div pt-typeahead data="filters.locality.data" selected="filters.locality.selection" selecteditem="filters.locality.selecteditem" add="add(item, model, label, type='locality')" update-filter="updateFilter(filter='locality', fetch='false', data)">
                                        <br>
                                        <div class="filter-selected">
                                            <button type="button" class="btn" ng-repeat="(id, item) in filters.locality.selection" ng-model="item" ng-click="remove('locality', id)">{{item}}</button>
                                        </div>
                                        <br>
                                        <div class="filter-recommended">
                                            <button type="button" class="btn" ng-repeat="(idx, item) in filters.locality.recommended" ng-model="item" ng-click="add(item, null, item.label, 'locality')">{{item.label}}</button>
                                        </div>
                                    </div>
                                </div>
                            </div>-->
                            <div class="column brdr-top" ng-if='!filters.builder.hide'>
                                <div class="builder-filter brdr-right">
                                    <label>Builders</label>
                                    <div id="b-tahead" pt-typeahead data="filters.builder.data" selected="filters.builder.selection" selecteditem="filters.builder.selecteditem" add="add(item, model, label, type='builder')" update-filter="updateFilter(filter='builder', fetch, data)">
                                        
                                        <div class="filter-recommended">
                                        	<a class="" ng-repeat="(id, item) in filters.builder.recommended" ng-click="add(item, model, label, type='builder')" ng-show="!item.notVisible"><span class="buider-ellipse" once-title="item.label" once-text="item.label"></span></a>
                                        </div>
                                        <br>
                                    </div>
                                </div>
                                <div class="filter-selected">
                                    <a class="btn btn-small-builer" href="" ng-repeat="(idx, item) in filters.builder.selection" ng-click="remove('builder', item)" ng-model="item"><span class="buider-ellipse" once-title="item" once-text="item"></span> <i class="fa fa-times"></i></a>
                                </div>
                            </div>
                            <div class="column brdr-top" ng-if='!filters.locality.hide'>
                                <div class="builder-filter brdr-right">
                                    <label>Localities</label>
                                    <div id="b-tahead" pt-typeahead data="filters.locality.data" selected="filters.locality.selection" selecteditem="filters.locality.selecteditem" add="add(item, model, label, type='locality')" update-filter="updateFilter(filter='locality', fetch, data)">
                                        
                                        <div class="filter-recommended">
                                            <a class="" ng-repeat="(id, item) in filters.locality.recommended" ng-click="add(item, model, label, type='locality')" ng-show="!item.notVisible"><span class="buider-ellipse" once-title="item.label" once-text="item.label"></span></a>
                                        </div>
                                        <br>
                                    </div>
                                </div>
                                <div class="filter-selected">
                                    <a class="btn btn-small-builer" href="" ng-repeat="(idx, item) in filters.locality.selection" ng-click="remove('locality', item)" ng-model="item"><span class="buider-ellipse" once-title="item" once-text="item"></span> <i class="fa fa-times"></i></a>
                                </div>
                            </div>
                        </li>
                    </ul>
                </li> 
                <li class="sec-btn" ng-click="saveSearchClicked()">
                    <button class="btn btn-small"><i class="fa fa-floppy-o"></i> &nbsp;Save Search</button>
                </li> 
                <li>
                    <button class="btn btn-small" ng-class="{disabled:!displaySettings.appliedFilter}" ng-click="clearAll()"><i class="fa fa-refresh"></i> &nbsp;Reset</button>
                </li>
                <li class="help-tips" ng-if="isMaps()">
                   <i id="usehelp" class="fa fa-lightbulb-o active" tooltip="Help Tips" ng-click="ShowAgainMap()"></i>
               </li>
           </ul>
           <div class="btn-group mapOrList filter-radio pull-right" ng-if="showFilters && cityMapSupport">
               <a><button type="button" class="btn listView" ng-class="{active : !isMaps()}" ng-model="mapOrList" btn-radio="'Map'" ng-click="toggleMapList(false)"><i class="fa fa-list-ul"></i> List</button></a>
               <a><button type="button" class="btn mapView" ng-class="{active : isMaps()}" ng-model="mapOrList" btn-radio="'Map'" ng-click="toggleMapList(true)"><i class="fa fa-map-marker"></i> Map</button></a>
           </div>
        
    </div>
</div>
